title: "Petrica — Portfolio"
description: "Game dev portfolio: projects, experiments, and demos."
lang: en

plugins:
  - jekyll-seo-tag

collections:
  projects:
    output: true
    permalink: /projects/:name/